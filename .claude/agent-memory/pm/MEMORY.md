# PM Agent Memory

## Architecture
- Blog + portfolio site (pivoted from financial dashboard 2026-03-26)
- SvelteKit frontend only, no backend
- Docker deployment on DigitalOcean with Caddy

## Conventions
- UI tasks only (no backend/data tasks)
- Blog content is markdown files, not database
- Arabic translations are separate .md files in src/lib/blog/ar/
